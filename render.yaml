services:
  - type: web
    name: chieu-nau-backend
    env: node
    plan: free
    region: singapore
    buildCommand: npm install
    startCommand: npm run server
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true
